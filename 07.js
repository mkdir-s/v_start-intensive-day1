
const methods = [
    'push()',
    'pop()',
    'shift()',
    'unshift()',
    'slice()',
    'splice()',
    'forEach()',
    'map()',
    'find()',
    'findIndex()'
  ];
  
methods.forEach(method => {
    console.log("Метод " + method);
});
  
  // Вывод 
  // Метод push()
  // Метод pop()
  // Метод shift()
  // Метод unshift()
  // Метод slice()
  // Метод splice()
  // Метод forEach()
  // Метод map()
  // Метод find()
  // Метод findIndex()